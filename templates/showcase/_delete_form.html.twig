<form method="post" action="{{ path('app_showcase_delete', {'id': showcase.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ showcase.id) }}">
    <button class="btn">Delete</button>
</form>
